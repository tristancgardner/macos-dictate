Private MBP/MBA sync note:
- Keep `.env.local`, `mappings.local.json`, and `KNOWN_ISSUES.md` out of the public `macos-dictate` repo.
- Store/sync those files through the private `dotfiles/macos-dictate/` setup, with local symlinks from this repo on both MacBook Pro and MacBook Air.
