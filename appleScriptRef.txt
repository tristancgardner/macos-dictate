-- Kill any existing dictate processes first
do shell script "pkill -9 -f 'dictate.py' 2>/dev/null || true"
delay 0.5

-- Run via Terminal (has Accessibility permissions)
tell application "Terminal"
    do script "/Users/tristangardner/.pyenv/versions/3.12.7/envs/venv/bin/python /Users/tristangardner/Documents/Programming/01_Apps/macos-dictate/dictate.py --model small"
    delay 1
    set miniaturized of front window to true
end tell

-- Hide Terminal completely
tell application "System Events"
    set visible of process "Terminal" to false
end tell
