# macos-dictate

## Python Environment

This project requires the pyenv virtual environment with PyObjC (Quartz) installed:

```
/Users/tristangardner/.pyenv/versions/3.12.7/envs/venv/bin/python
```

Use this Python executable when running `dictate.py` directly.
